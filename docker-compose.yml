version: '3.8'
services:
  app:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    volumes:
      - face_encodings:/app/static/face_encodings
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    expose:
      - "6379"
volumes:
  face_encodings:
